<div class="container guest-album-index">

  <div class="row-fluid">
    <div class="span12 title">
      <a href="#"><%= owner.get("business_name") %></a>
    </div>

    <div class="span12 categories">
      <% if (owner.get("categories")) { %>
        <% (owner.get("categories")).forEach(function (category) { %>
          <% if (category !== null ) { %>
            <a href="javascript.void(0)"><%= category %></a>
          <% } %>
        <% }) %>
      <% } %>
    </div>
  </div>

  <div class="row-fluid albums-container">


  </div>

  <div class="row-fluid footer">
    <footer>© <%= owner.get("business_name") %></footer>
  </div>
</div>
